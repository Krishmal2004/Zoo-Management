import React from 'react';
import PlaceholderScreen from '../shared/PlaceholderScreen';

export default function TicketShowPlaceholder() {
  return (
    <PlaceholderScreen
      emoji="🎟️"
      title="Ticket & Show Management"
      body="Book tickets and shows, upload school or group visit documents (PDF/images), and receive QR tickets from the system — coming in Phase 2."
    />
  );
}
